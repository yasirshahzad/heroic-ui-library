import React from 'react';
import { NotificationProps } from './Notification.types';
declare const Notification: ({ color, counter, mark }: NotificationProps) => React.JSX.Element;
export default Notification;
