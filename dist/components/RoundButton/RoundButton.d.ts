import React from 'react';
import { RoundButtonProps } from './RoundButton.types';
declare const RoundButton: ({ size, btnType, selected, disabled, className, ...props }: RoundButtonProps) => React.JSX.Element;
export default RoundButton;
